const initialState = {};

export const passwordNewReducer = (state: any = initialState, action: any): any => {
    switch (action.type) {
        default: {
            return state;
        }
    }
};