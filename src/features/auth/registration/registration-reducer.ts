const initialState = {};

export const registrationReducer = (state: any = initialState, action: any): any => {
    switch (action.type) {
        default: {
            return state;
        }
    }
};